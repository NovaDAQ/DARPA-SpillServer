# Runtime dependencies of darpa-spillserver, mirroring [project.dependencies]
# in pyproject.toml. bootstrap.sh installs these first, then the package
# itself in editable mode; tests/test_packaging.py keeps the two in step.
#
#   python -m pip install -r requirements.txt
#
# nova-time-decoder is a NOvA DAQ package; bootstrap.sh installs it from a
# sibling checkout when one exists and skips this line.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.26
pyyaml>=6.0
jinja2>=3.1
nova-time-decoder>=1.2.0
