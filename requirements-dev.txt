# Test and optional-feature dependencies: the [test] and [oidc] extras.
#
#   python -m pip install -r requirements.txt -r requirements-dev.txt
pytest>=7.0
pytest-asyncio>=0.23
respx>=0.20
authlib>=1.3
itsdangerous>=2.1
